apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: my-app
resources:
- deployment.yaml
- service.yaml
- configmap.yaml
labels:
- includeSelectors: true
  pairs:
    managed-by: argocd
    source: oci-registry
