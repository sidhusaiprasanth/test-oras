apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: sample-app

namespace: default

commonLabels:
  app: sample-app
  managed-by: kustomize

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: nginx
    newTag: "1.27"
